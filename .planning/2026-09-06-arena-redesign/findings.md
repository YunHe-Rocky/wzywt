# 调查与最终实现记录
- 保留 app/web → features → core 边界与工作区既有业务修改；首页使用现有 immediate/deferred 资源机制。
- 替换强制浅色厚玻璃规则，保留业务布局、表格、对话框和可访问性基线。
- 保留 /m 复用页面，实现手机和桌面双向中转，保留查询参数及静态资源访问，修复 /me、/monitor 前缀误判。
- 最终主视觉为 public/art/arena.webp，1984×793，127484 字节。地图放大且外沿不入镜，人物站在敌方一塔附近城墙并移至右下，清晰地形至中路后渐隐。
- 开场为实时 Canvas 网站组件，不是动画图片：星点组成盾剑徽记，支持首次播放、跳过、重播、减少动效、焦点与滚动恢复。
- 人因回归发现并修复新导航小字号；减少动效过渡设为 0，监控滚动区域聚焦即时显示轮廓。
- 完整检查日志、模拟数据浏览器截图与真实数据生产截图分别保存在 .cache/arena-redesign；该目录不提交。

- 用户明确偏好原来的玻璃光影和细腻动效。新增 arena-glass.css 恢复透明材质、边缘高光、低亮度环境光；CursorLighting 只处理鼠标所在表面，动态偏好变化和滚动均清理，手机采用触按与 Dock 滑动反馈。
- 精简首页与共享页文案，保留清晰功能名。首页主题句改为“今晚，峡谷见。”。
- 真实数据巡检发现英雄 SSE 早于租约就绪且 refetch 每渲染改变引用；现等待加载成功再订阅、稳定 refetch 并展示刷新失败恢复入口。错误与具体租约关联，410 自动重连后不残留旧失败提示。
