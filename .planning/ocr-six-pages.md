# Screenshot recognition repair

- Evidence: match worker sends six files; Python accepts only one DATA file.
- Live page at user-provided VM responds, but inspection is unauthenticated.
- Implement six verified landscape page layouts and bounded sequential batch inference.
- Preserve missing/ambiguous values and cross-page consistency/manual confirmation.
- Persist safe actionable failure codes; stop retrying invalid input/configuration.
- Verify Python HTTP/parser tests, Python→TypeScript contract, six real repository samples,
  transport tests, browser error rendering, architecture/types/core/lint/build.
- No database schema changes; website and independent OCR service need separate updates.

## Verification

- Implemented six templates, sequential bounded multipart batches and short-number recovery.
- Real engine + six-file HTTP: 60 scores and 180/180 independently transcribed statistics,
  12.8 seconds on this Windows machine; normalized into ten players, zero numeric conflicts,
  zero missing statistics, WARNING requiring manual confirmation. One hero label remains unknown.
- Python suite: 36 tests. Python→TypeScript single-page and six-page contracts pass.
- Connection/retry classification, architecture, core, next-stage and type checks pass.
- Lint: no errors, 18 existing img warnings. Production build: 42/42 static pages.
- Browser feedback regression: desktop 1280px, mobile 390px and 320px.
- Windows sandbox blocked cache writes/tsx user lookup/build startup; approved reruns passed.
- Live VM page is reachable but requires login; its uploaded evidence has not been inspected.
- No VM deployment, Git commit/push, schema/migration or credential changes made.
- Other concurrent changes to root planning files/artifacts were left untouched.
